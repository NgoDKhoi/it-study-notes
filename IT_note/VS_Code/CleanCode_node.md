# Chữ viết có thể không clean nhưng code thì phải clean

- Field, biến nội bộ trong class (thường là private) thì dùng `_camelCase`
- Biến, tham số -> `camelCase`
- class, hàm, property -> `PascalCase`
